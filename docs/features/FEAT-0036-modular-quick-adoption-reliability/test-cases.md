# FEAT-0036 Test Scenarios

Test implementation: capability-owned quick-adoption distribution and
consumer-update adapter fixtures.

| ID | Related slice | Scenario | Expected result | Level | Status | Automation |
| --- | --- | --- | --- | --- | --- | --- |
| `TEST-0150` | `SUBF-0069` | Create a canonical managed issue whose POST and direct identity reads succeed while the all-issues inventory still returns zero matches; separately expose one different canonical identity after creation. | Zero list matches continue from the exact created identity through draft-PR publication; the different visible identity fails closed before branch or PR publication. The issue-token author may differ from the updater PAT actor. | Lifecycle integration / eventual consistency / identity race / separate credentials | Passing locally | Canonical protocol-update adapter fixture and focused lag/race selector |
| `TEST-0147` | `SUBF-0067` | Inspect source/module inventory and public parameter parity; build the same exact source twice; exercise one valid immutable bundle and invalid release/digest/archive/manifest/payload variants; invoke one representative thin-bootstrap path on Windows PowerShell 5.1 and import the direct module on PowerShell 7. | Source is cohesive and generated artifacts are untracked; both builds are byte-identical; only the exact release-bound, path-safe, digest-complete bundle imports outside the consumer and preserves prior behavior; every invalid variant fails before the shared mutation sentinel. | Structure / deterministic build / supply chain / integration / cross-runtime / negative | Passing locally | New quick-adoption distribution owner plus retained launcher slice |
| `TEST-0148` | `SUBF-0068` | Run the production adapter through a native fake GitHub CLI whose explicit GET fails once or twice with transport/408/429/5xx evidence, always fails transiently, or fails with 401/403/404/422/invalid JSON; also exercise paginated GET and POST/PATCH/DELETE calls. | Retryable idempotent reads converge within three total attempts; exhaustion preserves the final diagnostic; permanent/semantic failures and every mutation execute once; no branch, issue, PR, or default-branch mutation occurs because of read retry. | Native-boundary integration / retry / failure / mutation safety | Passing locally | Focused protocol-update reliability owner plus real native helper |
| `TEST-0149` | `SUBF-0069` | Send canonical issue, comment, PATCH, and pull-request bodies with JSON through a real Windows PowerShell 5.1 native executable boundary; seed the exact quote-stripped schema-2 issue and near matches varying marker, fields, prose, author, duplication, branch, and PR state. | Every canonical body round-trips byte-for-byte without BOM/argv corruption and temporary files are removed; only the exact trusted orphan issue is rewritten through safe transport, refetched, and parsed canonically; every near match blocks before mutation. | Native-boundary regression / ownership / recovery / security / negative / idempotency | Passing locally | Focused protocol-update reliability owner plus real native helper |

## Evidence

| Date | Commit | Environment | Command | Result |
| --- | --- | --- | --- | --- |
| 2026-07-20 | `f17638f1c4223620e4c13d67b451808e321f4416` baseline | Windows PowerShell 5.1 / live Derdini evidence | v0.12.3 quick adoption against the exact Derdini default head | Expected production failure establishing `BUG-0018`: one transient `GET /git/trees/{sha}` `connectex` error terminated target-bound recovery; rerun established `BUG-0019`: issue #7 contained quote-stripped schema-2 ownership text and blocked canonical parsing |
| 2026-07-20 | Test-first FEAT-0036 working tree | Windows PowerShell 5.1 | `powershell -NoProfile -ExecutionPolicy Bypass -File tests/capabilities/initial-adoption/quick-adoption-bundle.tests.ps1` | Expected red in 1.0 seconds: the builder, ordered source inventory, module manifest/loader, six private source slices, public entry point, runtime trust constants, and bounded thin-launcher surface were absent; the existing launcher remained 6,368 lines |
| 2026-07-20 | Test-first FEAT-0036 working tree | Windows PowerShell 5.1 | `powershell -NoProfile -ExecutionPolicy Bypass -File tests/capabilities/consumer-update/protocol-update-reliability.tests.ps1` | Expected red in 0.9 seconds: the adapter lacked the bounded GET-read helpers, UTF-8-no-BOM body-file helpers, exact quote-stripped issue predicate/repair, and repair-before-label preflight; structured bodies were still present on native argv |
| 2026-07-20 | Final reviewed FEAT-0036 working tree | Windows PowerShell 5.1 | `powershell -NoProfile -ExecutionPolicy Bypass -File tests/capabilities/initial-adoption/quick-adoption-bundle.tests.ps1` | Passed `TEST-0147` in 17.7 seconds; exact transform-sensitive Git-blob builds, public parity, private-token fallback, immutable release/bundle trust, archive containment, module import, mutation sentinel, preflight order, environment restoration, and real post-write cleanup passed |
| 2026-07-20 | Final reviewed FEAT-0036 working tree | Windows PowerShell 5.1 | `powershell -NoProfile -ExecutionPolicy Bypass -File tests/capabilities/consumer-update/protocol-update-reliability.tests.ps1` | Passed `TEST-0148` and `TEST-0149` in 23.4 seconds; exact GET-only retry, body-file transport, cleanup, call-site, drift, near-match, ordinal branch, and idempotent-rerun matrices passed, including repair of the stale v0.12.1 poisoned issue while targeting v0.12.4 |
| 2026-07-20 | Final reviewed FEAT-0036 working tree | Windows PowerShell 5.1 | `powershell -NoProfile -ExecutionPolicy Bypass -File tests/capabilities/publication-evidence/post-publication-evidence.tests.ps1` | Passed the focused publication-evidence owner in 1.7 seconds without claiming published state; exact two-asset inventory, bounded pre-download metadata, launcher/source bytes, downloaded digests, regular ordered bundle entries, manifest/source identity, and negative variants support `TEST-0147` |
| 2026-07-20 | Final reviewed FEAT-0036 working tree | Windows PowerShell 5.1 | `powershell -NoProfile -ExecutionPolicy Bypass -File tests/capabilities/consumer-update/protocol-update.tests.ps1` | Passed the canonical 27-scenario consumer-update suite in 54.5 seconds, including replacement, migration, release proof, credentials, pagination, races, leases, compensation, and native-stderr regression |
| 2026-07-20 | Final reviewed FEAT-0036 working tree | Windows PowerShell 5.1 | Quick-adoption `ContractsPreflight`, `CurrentLauncherRecovery`, and streaming `WindowsNative` shards | Passed in 15.7, 4.4, and 5.0 seconds respectively; sandbox-only Git signal-pipe failures were rerun on the real Windows host boundary |
| 2026-07-20 | Final reviewed FEAT-0036 working tree | Windows PowerShell 5.1 | `tests/protocol.tests.ps1 -StructureOnly`, changed PowerShell AST parse, and `git diff --check` | Passed; protocol governance, current-version pins, scenario ownership, 18 changed PowerShell files, and whitespace converged without a full local suite rerun |
| 2026-07-20 | PR #90 head `003f5fce674db1d4b6830fc7bf9a4f222358c763` | GitHub-hosted Windows PowerShell 5.1 and PowerShell 7 | Protocol validation run `29746595925` | Expected delivery-gate failure exposing `FIND-0192`: the legacy finalization fake rejected the new `api user` read and treated `body=@file` as inline text; production recovery had not executed yet |
| 2026-07-20 | Post-`FIND-0192` working tree | Windows PowerShell 5.1 | `powershell -NoProfile -ExecutionPolicy Bypass -File tests/capabilities/consumer-update/managed-merge-finalization.tests.ps1` | Passed in 11.1 seconds; `TEST-0108`, `TEST-0109`, `TEST-0110`, `TEST-0112`, and `TEST-0142` now exercise authenticated actor lookup and byte-exact body-file decoding through the finalization fake |
| 2026-07-20 | PR #90 head `9a5accaaf0d9d783a2a7f72dc03f66f71f9ec4a3` | GitHub-hosted PowerShell 7 on Ubuntu | Protocol validation run `29747828300` | Expected delivery-gate failure exposing `FIND-0193`: the consumer-contained temp negative changed only Windows `TEMP/TMP`, while Unix `.NET GetTempPath()` continued to use `TMPDIR`; all preceding focused updater suites passed |
| 2026-07-20 | Post-`FIND-0193` working tree | Windows PowerShell 5.1 | `powershell -NoProfile -ExecutionPolicy Bypass -File tests/capabilities/initial-adoption/quick-adoption-bundle.tests.ps1` | Passed `TEST-0147` in 18.1 seconds after the fixture began setting and restoring `TEMP`, `TMP`, and `TMPDIR` and asserted that the unsafe process temp really activated; hosted Ubuntu remains the final Unix execution proof |
| 2026-07-20 | PR #90 head `9a5accaaf0d9d783a2a7f72dc03f66f71f9ec4a3` | GitHub-hosted Windows PowerShell 5.1 | Protocol validation run `29747828300` | Expected delivery-gate failure exposing `FIND-0194`: after TEST-0147 and the focused updater suites passed, the full adoption-lifecycle fixture labeled a `VERSION=0.12.3` snapshot as `v0.12.4` and the exact source gate rejected it |
| 2026-07-20 | Post-`FIND-0194` / `FIND-0195` working tree | Windows PowerShell 5.1 outside the sandboxed Git signal-pipe boundary | `powershell -NoProfile -ExecutionPolicy Bypass -File tests/capabilities/initial-adoption/quick-adoption.tests.ps1 -Shard AdoptionLifecycle` | Passed in 170 seconds after the sole stale snapshot version was corrected and the mock `gh repo clone` began judging raw Git by exit code rather than informational stderr; the sandbox-only first attempt stopped at the known Git signal-pipe restriction before scenario execution |
| 2026-07-20 | Post-`FIND-0196` hotfix working tree | Windows PowerShell 5.1 | `powershell -NoProfile -ExecutionPolicy Bypass -File tests/capabilities/initial-adoption/quick-adoption-bundle.tests.ps1` | Passed `TEST-0147` in 21.3 seconds; the fixture builder was launched by a real Windows PowerShell 5.1 `-File` process without `-SourceRoot`, and its exact-source archive matched both explicit-root deterministic builds |
| 2026-07-20 | Immutable v0.12.4 / Derdini `main` `96a9902e8f035bac2f638182efd3c940d72cc101` | Released Windows PowerShell 5.1 launcher | `Invoke-MeAndAIQuickAdoption.ps1 -TargetPath . -ProtocolTag v0.12.4` | Expected production failure exposing `FIND-0197` at 70% after 83.3 seconds: issue #7 was repaired to exact canonical JSON and issue #8 was created with exact v0.12.4 ownership, but immediate all-issues visibility returned no match; no recovery branch or replacement PR was published and local main remained unchanged |
| 2026-07-20 | v0.12.5 `FIND-0197` candidate | Windows PowerShell 5.1 | `protocol-update-adapter.fixture.ps1 -CreatedIssueLagOnly` | Passed `TEST-0150` in 12.0 seconds: a created issue remained hidden through the first direct read and immediate zero-match list, then reached draft-PR creation despite its `github-actions[bot]` issue author differing from the updater PAT actor; one different visible canonical identity failed closed before PR creation |
| 2026-07-20 | v0.12.5 `FIND-0197` candidate | Windows PowerShell 5.1 | Focused updater/bundle/publication/contract owners | `TEST-0148` and `TEST-0149` passed in 23.5 seconds; `TEST-0147` passed in 20.0 seconds; publication evidence passed in 1.9 seconds; capabilities-bootstrap `Contracts` passed in 1.9 seconds |
| 2026-07-20 | v0.12.5 `FIND-0197` candidate | Windows PowerShell 5.1 outside the sandboxed Git signal-pipe boundary | `tests/capabilities/consumer-update/protocol-update.tests.ps1` plus focused registry check | All 28 behavior scenarios passed in 64.0 seconds, including true zero-list lag, different-identity race, replacement-first supersession, rollback, pagination, release identity, migration, and atomic gates; after correcting the two new assertion IDs from the pre-existing `TEST-0111` to `TEST-0150`, exact scenario ownership binding passed in 0.7 seconds without rerunning unchanged behavior |

The v0.12.5 PR, release, successful Derdini replay, and cleanup evidence remain
pending; local focused scenarios do not predict those external facts.
